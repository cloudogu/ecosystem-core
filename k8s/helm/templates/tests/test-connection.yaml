apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ecosystem-core.fullname" . }}-test-connection"
  labels:
    {{- include "ecosystem-core.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ecosystem-core.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
